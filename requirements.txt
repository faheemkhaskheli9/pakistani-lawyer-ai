pypdf>=4.0
numpy>=1.24

# Optional: only needed for EMBEDDING_PROVIDER=sentence-transformers; the
# default "hashing" provider needs neither this nor a model download.
# sentence-transformers>=2.2

pytest>=8.0
